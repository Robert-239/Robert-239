import numpy as np 
from matplotlib import pyplot as plt

print('hello world')
j = 12
def randomArray(n):
    arr = np.array([])
    for i in range(1,n+1):
        arr[i] = np.random
    return arr
print(randomArray(j))
